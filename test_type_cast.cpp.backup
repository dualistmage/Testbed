#include <iostream>

using namespace std;

int main()
{
    int a = 3, b = 5;
    cout << "Type A : " << static_cast<float>(a / b) << endl;
    cout << "Type B : " << static_cast<float>(a) / static_cast<float>(b) << endl;
    return 0;
}
