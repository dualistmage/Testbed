#include <iostream>

using std::cout;
using std::endl;

class TEST
{
    public:
        int num;
}; // end - TEST

int main()
{
    std::pair< int , int > p1, p2;
    p1.first = 2; p1.second = 3;
    p2.first = 2; p2.second = 3;
    if ( p1 == p2 )
        cout << "Same" << endl;
    else
        cout << "Different" << endl;

    std::pair< int , TEST> p3, p4;
    p3.first = 2; p3.second.num = 3;
    p4.first = 2; p4.second.num = 3;
    // Compilation Error
    // ---------------------------------
    // if ( p3 == p4 )
    //     cout << "Same" << endl;
    // else
    //     cout << "Different" << endl;
    // ---------------------------------

    return 0;
} // end - main()
