#/bin/sh
if [ $# -eq 3 ]
then
	find . -name "$3" -type f | while read file
	do
		iconv -f $1 -t $2 < $file > $file.tmpiconv;
		mv $file.tmpiconv $file;
	done
	exit
else
	echo "[Usage] recur-cvt <Input Encoding> <Output Encoding> <File name or regexp>"
	exit
fi
