///
/// @file test_std_enum.cpp
/// @brief Test code of enum.
/// @author Dohyun Yun ( dualistmage@nate.com )
/// 

#include <iostream>

enum TEST_ENUM {
    N1 = 1,
    N2 = 2,
    N3 = 4,
    N4
};

int main() 
{
        using namespace std;

        TEST_ENUM x,y,z,k;
        x = N1;
        y = N2;
        z = N3;
        k = N4;
        cout << x << " - " << y << " - " << z << " - " << k << endl;
}
